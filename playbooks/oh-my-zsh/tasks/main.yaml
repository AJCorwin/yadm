---
- name: Checking for existing install
  stat:
    path: "$HOME/.oh-my-zsh"
  register: install
  become: true

- name: Install Oh-My-ZSH 
  shell: "$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)" ""  --unattended
